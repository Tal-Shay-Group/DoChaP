angular.module("DoChaP").service("compareSpeciesService", function ($http, $window, $rootScope,webService) {
    this.specieTextInput="";
    this.searchGene=function(textInput,specie1,specie2){

    };

})